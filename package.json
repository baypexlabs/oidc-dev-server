{
  "name": "oidc-dev-server",
  "version": "0.0.1",
  "description": "OIDC Provider for use in Devcontainers",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "run": "node src/index.ts"
  },
  "dependencies": {
    "express": "^5.2.1",
    "oidc-provider": "^9.7.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/oidc-provider": "^9.5.0",
    "eslint": "^10.1.0",
    "gts": "^7.0.0",
    "prettier": "^3.8.1",
    "typescript": "^5.6.3"
  }
}
